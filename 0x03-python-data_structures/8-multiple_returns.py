#!/usr/bin/python3
def multiple_returns(sentence):
    lenght = len(sentence)
    if length == 0:
        first_chr = None
    else:
        first_chr = sentence[0]
    data = (lenght, first_chr)
    return data
